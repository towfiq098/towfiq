#include<stdio.h>
int main(){
    FILE *f;
    char ch;
    f=fopen("selfprint.c","r"); 
    if(f==NULL){
        printf("Cannot open file.\n");
        return 1;
    }
    while((ch=fgetc(f))!=EOF){
        putchar(ch);
    }
    fclose(f);
    return 0;
}
