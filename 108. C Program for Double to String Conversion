#include<stdio.h>
#include<stdlib.h>
int main(){
    double num;
    char str[100];
    printf("Enter a double value:");
    scanf("%lf",&num);
    sprintf(str,"%lf",num); 
    printf("Double to String: %s\n",str);
    return 0;
}
