#include<stdio.h>
int*getMax(int *a,int *b) {
    if(*a>*b)
        return a;
    else
        return b;
}
int main(){
    int x,y;
    printf("Enter two numbers:");
    scanf("%d %d",&x,&y);
    int *max=getMax(&x,&y);
    printf("Larger number: %d\n",*max);
    return 0;
}
