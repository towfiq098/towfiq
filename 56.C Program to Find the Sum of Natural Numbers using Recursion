#include <stdio.h>
int sum(int n) {
    if(n==1)
        return 1;          
    else
        return n+sum(n-1);
}
int main(){
    int num;
    printf("Enter a positive number:");
    scanf("%d",&num);
    printf("Sum of natural numbers=%d\n",sum(num));
    return 0;
}
