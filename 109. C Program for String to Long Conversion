#include<stdio.h>
#include<stdlib.h>
int main(){
    char str[50];
    long num;
    printf("Enter a number string:");
    scanf("%s",str);
    num=atol(str); 
    printf("String to Long: %ld\n",num);
    return 0;
}
